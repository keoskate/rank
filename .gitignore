# Logs
logs
*.Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Compiled directory and file
build/
dist/
bundle.js
*.bundle.js
*.chunk.js
*.map
*.min.js

# All bundle and build files
**/*.bundle.js
**/*.chunk.js
**/*.bundle.map
**/*.chunk.map

# Build outputs
react-client/dist/
react-client/build/

# Dependency directories
node_modules/

# Environment variables (contains API keys - NEVER commit!)
.env
.env.local
.env.development
.env.development.local
.env.test.local
.env.production
.env.production.local

# Linting and formatting cache
.eslintcache
.prettierrc.local

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# AI session data (may contain trading info)
data/ai-sessions.json

# Session/improvement data backups (regenerated, can be large)
data/*.bak
data/*.bak[0-9]*
data/ai-sessions.json.backup.*
data/improvement-log.json

# CheddarFlow cache and debug files
data/cheddarflow-*.json
data/cheddarflow-*.png

# Scanner + analyst runtime outputs (regenerated each run; keep .gitkeep)
data/scanner-results/*.json
data/stock-analyses/*.json

# UI inspection artifacts (Playwright screenshots + logs)
data/ui-inspect/*.png
data/ui-inspect/*.json

# Training data (personal trade history)
data/training/*.json

# Legacy/internal documentation (not needed for public repo)
docs/internal/
*.local.md

# Temporary files
*.tmp
*.temp
.cache/

# E2E test screenshots (generated, can be large)
tests/screenshots/

# Server backup files
server/*.bak

# Log files
*.log
server.log
react-client/*.log
data/logs/

# Trading logs
data/logs/trading.log

# Claude Code config (keep agents/ and commands/ tracked for team sharing)
.claude/*
!.claude/agents/
!.claude/commands/

scripts/backtests/.overlays-bars-cache.json
.overlays-bars-alpaca.json

# Backtest run artifacts + adjusted-bars cache (generated by scripts/backtests)
# …but keep the small provenance files versioned: the trials ledger is the
# honest N for Sharpe deflation, and certifications prove live==backtest.
data/backtests/*
!data/backtests/trials-ledger.json
!data/backtests/certifications/
!data/backtests/manifests/
.agent-quarantine/

# Generated market/runtime data (regenerable by the running server — do not bloat git)
data/darkpool-archive/
data/flow-history/
data/recovery/
data/daily-history.json
data/*.tmp-*

# Python research layer (Phase 1 GBDT ranker; Phase 2 Kronos) — venv is ~240MB
python/research/.venv/
__pycache__/
*.pyc

# Regenerable rank-score caches: exported bars + OOS scores + certs
# (rebuild with: npm run rank:export && npm run rank:scores)
data/rank-cache/

# Integrity layer output (regenerated every 60s)
data/reports/integrity/
